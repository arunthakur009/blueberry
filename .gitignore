# Build artefacts — generated by make, never committed
obj/

# Machine-local build config (see Make.config for defaults)
Make.local

# Editor cruft
*.swp
*.swo
.DS_Store
*~

# Go build cache
src/bpm/bpm
*.test

# Signing keys — never commit these
*.key
*.minisig
