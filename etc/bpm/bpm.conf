# Blueberry Package Manager — runtime configuration

# Filesystem root for package installation
# root = "/"

# Cache directory for downloaded packages and indices
# cache_dir = "/var/lib/bpm/cache"

# Repository configuration directory
# repos_dir = "/etc/bpm/repos.d"

# Installed-package database directory
# db_path = "/var/lib/bpm/db"
