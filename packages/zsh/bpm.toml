[package]
name     = "zsh"
version  = "5.9"
release  = 1
summary  = "A powerful shell with scripting language and command-line completion"
homepage = "https://www.zsh.org/"
license  = ["custom:zsh"]
arch     = ["x86_64"]

depends     = ["glibc", "ncurses", "libcap", "gdbm"]
makedepends = ["pkgconf"]

[[source]]
url    = "https://downloads.sourceforge.net/project/zsh/zsh/5.9/zsh-5.9.tar.xz"
sha256 = "9b8d1ecedd5b5e81fbf1918e876752a7dd948e05c1a0dba10ab863842d45acd5"

[steps]
build = '''
cd "$name-$version";
# ncurses 6.5 ships boolcodes/numcodes/strcodes and the terminfo *names as real
# (extern) symbols, but zsh 5.9's configure probe fails to detect them, so it
# compiles its own local arrays that then clash with term.h. Tell zsh the
# symbols exist (HAVE_*) so it uses ncurses' and skips the local fallbacks.
export CFLAGS="${CFLAGS:-} -Wno-error -DHAVE_BOOLCODES -DHAVE_NUMCODES -DHAVE_STRCODES -DHAVE_BOOLNAMES -DHAVE_NUMNAMES -DHAVE_STRNAMES";
./configure --prefix=/usr --bindir=/usr/bin --sysconfdir=/etc/zsh --enable-etcdir=/etc/zsh \
  --enable-zshenv=/etc/zsh/zshenv --docdir=/usr/share/doc/zsh --enable-multibyte \
  --enable-function-subdirs --enable-fndir=/usr/share/zsh/functions --with-tcsetpgrp \
  --enable-cap --enable-gdbm --with-term-lib='ncursesw';
make
'''
package = '''
cd "$name-$version";
make DESTDIR="$pkgdir" install;
install -Dm644 LICENCE "$pkgdir/usr/share/licenses/$name/LICENCE"
'''
